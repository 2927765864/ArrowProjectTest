# Work Log

## 2026-04-08

- 初始化本地 Git 仓库。
- 添加 `.gitignore`，忽略构建产物和工具包目录：`.build/`、`.tools/`、`*.apk`。
- 建立本记录文件，后续每次我代你修改代码并提交时，会补一条简要说明。
- 提交测试：仅更新本记录文件，验证本地提交流程正常。
- 将主角形象从“史蒂夫”方块模型改为较小、偏 Q 版的小人（Capsule 和 Sphere 构成）。
- 调低了角色在移动和闲置时的基准高度（y 从 0.6 改为 0.25），使其符合新的短小体型。
- 更新 ControlPanel 面板版本号为 v2026.04.08-1000。
- 使用 apktool 将 Android 应用打包，并将 Android 系统的应用名 (app_name) 同样设为版本号 v2026.04.08-1000。
- 放大地图整体尺寸 (`ARENA_HEIGHT` 从 32 增至 48)，横向视野也会按比例扩大。
- 将摄像机距离由 30 拉远至 60，并同步放大了直射光阴影覆盖范围。
- 调整了摄像机的裁切逻辑，从固定高改为“适应宽”(基于地图宽度+5%边距计算尺寸)。这确保在竖屏不同比例下，地图左右两侧边界始终能够完整显示不被裁切。
- 打包新安卓客户端 (包含放大地图+相机自适应宽度的修改)。
- 版本号由 v2026.04.08-1000 变更为 v2026.04.08-1001。
- 为主角（小人）添加了一顶红色的鸭舌帽，使用半球体作为帽檐上方主体，并使用压扁的圆柱体作为鸭舌边缘。
- 更新版本号至 v2026.04.08-1002，并重新构建包含了“主角红帽子”特性的最新安卓 APK。
- 使用 `<details>` 和 `<summary>` 对过于庞大复杂的参数面板 (Control Panel) 进行了重构和分类折叠。
- 划分为：🎮 游戏与模式、🏃 角色与移动、⚔️ 攻击与回收、⚡️ 连线特效、📳 震动设置、✨ 视觉后期与音效、🖥 UI 与界面、📊 数据图表 (预留) 八个清晰的模块。
- 添加了配套的 CSS 样式（圆角、下划线、箭头动画），为后续添加二维图表界面腾出了专属的可视化模块空间。
- 在控制面板预留的图表区域实现了一个原生的 2D 实时数据图表 (`SpeedChart.js`)。
- 每帧读取玩家 `currentVelocity.length()` 速度值并绘制出带半透明填充的动态折线图，用于直观查看主角移动的加速与惯性反馈。
- 放开了 `Input.js` 中 `pointerdown` 对鼠标类型的屏蔽 (`event.pointerType === 'mouse'`)。
- 现在在桌面端可以长按鼠标左键并拖拽来完全模拟移动端的虚拟摇杆操作，方便在 PC 浏览器进行测试。
- 优化了测速图表 `SpeedChart.js`，移除了动态计算 Y 轴最大值的逻辑。
- 现在纵坐标使用定死的上限（15），并增加了固定位置的网格参考线（刻度 5 和 10）以及文字标识，使速度变化在视觉上的比例更加稳定可读。
- 对虚拟摇杆手感进行了 Trick 优化。将摇杆原本的物理半径解耦为“视觉半径” (48px) 和“逻辑半径” (28px)。
- 玩家只需要手指滑动视觉半径的大约一半距离（逻辑半径），角色就能达到满速状态。但 UI 上摇杆帽依然能够跟着手指被推到更远的物理边界。这样操作更跟手且符合直觉，不易断触。
- 更新版本号至 v2026.04.08-1003，并打包安卓客户端。
- 此版本包含：控制面板折叠优化、摇杆手感双重半径Trick优化、以及全新的 Canvas 实时移动速度折线图。
- 在 `style.css` 中隐藏了玩家头上显示羽毛状态的四个球球 UI（`#player-hud { display: none; }`）。
- 在控制面板的 "🏃 角色与移动" 下新增 `最大移动速度` 参数（`maxMoveSpeed`），取代了代码里的硬编码 `10`。
- 在 "🎮 游戏与模式" 下新增 `相机视野缩放` 参数（`cameraViewScale`，范围 `1.0 ~ 3.0`），保证地图边界至少在 1.0 时囊括全屏，通过调大这个值可以随时将视角拉远，看到更多的场景外留白。
- 将 `相机视野缩放 (cameraViewScale)` 的下限从 1.0 修改为 0.3，允许玩家将镜头拉近（画面放大），即使这意味着地图的边界将超出屏幕外。
- 在场景地面中利用 CanvasTexture 动态生成了一个黑白棋盘格材质。
- 在控制面板 "🎮 游戏与模式" 选项卡中加入了“地面材质”的切换按钮，以支持在“纯色”和“黑白格子”间来回切换，方便研究和量化玩家的移动速度和距离。
- 新增 "障碍测试" (obstacles) 场景模式：在这个模式下会在地图中央和四周生成多块实体阻挡物 (BoxGeometry)。
- 在玩家移动核心逻辑 `updatePlayerMovement` 中加入了 AABB 碰撞检测机制。
- 当玩家与障碍物相撞时，移动速度在对应轴向将被阻挡清零，并在玩家头上浮现 "接触障碍" 的红字，同时伴随轻微屏幕震动（加入了 0.5s 防文字刷屏的 CD）。
- 将 UI 侧的“场景模式”从单纯的 无尽/空旷 升级成了 无尽 -> 障碍测试 -> 空旷 三档循环切换，并且我把初始状态设置为了 "障碍测试" 方便你进去直接测试。
- 修复了玩家接触障碍物时 `triggerShake is not defined` 导致游戏主循环崩溃（画面定格卡死）的严重 Bug（补充了确实的函数 import）。
- 修复了控制台 `favicon.ico 404 Not Found` 的无效网络请求报警。
- 优化了玩家与障碍物的 AABB 碰撞检测。将判定的物理半径从 `0.5 * playerScale` 大幅缩小到了 `0.2 * playerScale`。
- 这样改动后，碰撞箱只包围小人实际的模型身体部分（头部半径约 0.22），而不再把脚下的巨大虚线提示光圈（半径 0.45）算作碰撞体积，角色移动体验会更加符合视觉直觉且不容易卡墙。
- 更新版本号至 v2026.04.08-1004。
- 构建包含隐藏 UI、添加新障碍测试地图、修复触发震动 Bug 以及缩减玩家实际碰撞盒（AABB 胖子判定）等所有最新改动的安卓客户端。
- 移除了玩家接触障碍物时弹出的 "接触障碍" 浮动文字以及伴随的相机抖动效果，使碰撞逻辑更干净纯粹。
- 结束了 `dev_server.py` 监听 9000 端口的 Python 后台进程，释放了系统资源。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1005`。
- 构建新的安卓安装包时，统一只保留最新一版 APK，旧包会在出新包后清理。
- 修复参数面板里重复渲染两组“游戏与模式”控件的问题，移除了重复的 `id="inp-cvs"` / `id="val-cvs"` / `id="btn-scm"` / `id="val-scm"` 节点。
- 这会修复 Android WebView 中“相机视野缩放”滑杆命中错误节点、导致无法调节的问题。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1006`。
- 在参数面板“游戏与模式”中新增“相机跟随”按钮，可以随时开关相机跟随玩家的纵向移动。
- 优化了参数面板的视觉和布局：加宽面板、增强滚动区域、修复 flex 子项滚动失效，并提升分组卡片与控件的可读性。
- 将分组折叠改为手风琴模式，同一时间只展开一个分组，避免多个长分组同时展开时互相挤压、遮挡看不全。
- 将参数面板进一步调整为更接近 Unity Inspector 的结构：顶部横向标签切换分类，下面纵向展示当前分类内容，避免长列表折叠组来回挤压。
- 修复移动端网页/WebView 下参数面板无法横向和纵向滑动的问题：缩小全局 `touch-action: none` 的影响范围，并彻底切断控制面板原生触摸事件向游戏引擎底层的冒泡。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1009`。
- 优化玩家脚底移动方向指示器的视觉反馈：最大延伸半径再次收缩至 1.0 个玩家身位；且优化了摇杆映射逻辑，当摇杆中心小球推到物理边缘时指示器尚未达到最大范围，玩家手指需继续向外滑动一段距离，指示器才会完全展开至最大半径。
- 全面重构游戏色彩体系：将所有材质、特效、光源、背景及 UI 统一降维为“藤紫色”（`#91c53a`）和“钛啡绿”（`#5e55a2`）。
- 优化外发光边缘检测方案：移除通过在各个小模型内注入 `EdgesGeometry` 导致的“线框图”既视感，转而在全局渲染后处理中接入正统的 `OutlinePass` 轮廓识别通道，使得玩家、羽毛、敌人和障碍物呈现出最完美的纯色底+**纯外部包裹描边**（Cel-Shading/Vector Silhouette），而不再会因为模型内拼接看到穿插的杂线。
- 修复 `MeshBasicMaterial` 使用导致 `Enemy.js` 材质方法报错致死的致命 BUG。
- 修复漏网的 `addEdgeOutline` 导致的敌人生成报错。
- 优化主角卡通猫猫头造型：整体缩小头部以获得更好的头身比；增加了一条带有待机与移动摇摆动画的猫尾巴，且将后脑勺、耳朵和尾巴的颜色加深（`#7aa531`），以此在纯色渲染风格下建立明显的前后朝向视觉区分。
- 修复修改猫猫尾巴摆动动画时意外导致的 JavaScript 花括号 SyntaxError 崩溃报错。
- 彻底重塑玩家猫猫头（精细化）：利用多几何体嵌套（球体结合脸颊、耳内侧、立体胡须）组合成更加饱满、结构更立体的“胖脸猫”头型；配合 `OutlinePass` 渲染，这些内部几何体在重叠时不会产生破碎的线框，而是完美融合成一张精美、纯净的手绘矢量风猫脸剪影。
- 撤销对主角身体模型大改的 Bongo Cat 版本（因风格不太融合），将角色模型退回到上一个包含立体圆饼猫脸及胡须的版本（`v2026.04.09-1019`），保留精细化的脸型。
- 重构玩家角色（卡通猫猫）配色方案：从受限的双基色中独立出来，赋予其专属的“奶油布偶猫 (Cream Cat)”主题色。包含：面部及四肢前侧的暖奶油白 (`#fff5e6`)、代表阴影/背面/腿部/尾巴的摩卡色 (`#d4c2b2`)、粉嫩的内耳廓与小鼻头 (`#ff9eab`)、以及深咖啡色的眼睛与胡须 (`#362c28`)。这种配色与钛啡绿/藤紫色环境形成了强烈的层次与焦点对比。
- 优化主角配色表现：为了保证极致干净的赛璐璐视觉效果，移除了身体前后半球、腿部以及耳朵背面的摩卡色阴影区分。现在主角的身体、脸部和四肢统一采用了完整的奶油白（`#fff5e6`）纯色。这样消除了拼缝带来的视觉突兀感，轮廓线的勾勒（OutlinePass）将会完美融合成一整块柔软的面团质感。
- 优化了全局的悬浮伤害文本（Floating Damage Text）表现，去除了粗糙僵硬的黑色实体边框，转而使用更为柔和清爽的发光、半透明投影效果来衬托文本，大幅提升伤害数字跳出时的美观度与融入感。
- 微调了玩家的猫头和身体比例：收缩了脸颊的膨胀幅度使脸型更加秀气；下移并向内收紧了角色的双臂挂载位置，解决了之前“双手仿佛直接长在脑袋上”的错位问题，使其看起来更自然。
- 彻底移除飘字旁边图标的黑色死硬边框（通过修改 `drop-shadow` 滤镜），使得如护盾抵消等附加特效的图标与前面柔和发光的文字特效风格保持高度统一。
- 重新设计了敌人的造型模型：抛弃了带棱角的多面体和恶魔角，转为更平滑、更软糯的“恶魔猫猫团子”。保留了标志性的钛啡绿（紫黑）身躯和藤紫发光猫瞳，同时加入了尖立的恶魔猫耳和非常 Q 弹的果冻跳跃动画（Squash and Stretch Bounce）。
- **玩家武器模型重塑**：彻底重构了主角抛出的武器模型，由原本相对简单的羽毛替换为了极其酷炫的**朗基努斯之枪（Spear of Longinus）**造型。新模型拥有经典的笔直长柄、螺旋缠绕的双螺旋枪身以及顶端分叉的双刺枪尖；同时配合现有的纯色赛璐璐渲染及黑色完美勾边，呈现出极具视觉张力的漫画/机甲风攻击表现。
- 优化朗基努斯之枪（Spear of Longinus）模型及逻辑：修复了投掷出去时长枪首尾方向颠倒（倒着飞）的错误；重制了枪身模型结构比例，极大收紧了双螺旋结构缠绕的缝隙和半径，并将顶部的双叉枪尖大幅度延长，并在末端还原了内收的弧度，使外观更接近原作的流线与压迫感。
- 修正朗基努斯之枪在不同阶段的模型朝向：修复了收回时倒着飞（枪柄朝前）的问题，现在回收时长枪将会**枪尖朝内**飞回主角；同时更改了长枪落地时的状态，由原本如标枪般扎入地下，改为极具仪式感的**枪尖朝上**（并带有自旋倾斜）插立在地面上；此外还修复了长枪在飞行过程中的钻头自转轴错误。
- 重塑武器飞行的旋转与位姿逻辑：完全移除了导致“回旋镖”视觉错觉的花式空中自转动画，采用100%静态的三级定相旋转。投掷飞行时，长枪绝对笔直且枪尖朝向飞行轨迹前方；插地召唤时，剥离了所有的随机倾斜，长枪会以极其规整的形态直立，枪尖笔直指向天空；回收时，长枪会再次180度调头，枪尖对准主角直直飞回。
- 进一步精确化玩家抛射武器（朗基努斯之枪）：参照经典的结构设定图，重新用分段多边形拼贴方式实现了枪头部分标志性的、带机械菱角的“Z字形双曲折弯”与超长刺尖，替代了上一版本过于圆润的曲线；螺旋枪身被彻底改写为一根高度压缩并深度扭转的渐变锥体，使双螺旋槽纹紧紧贴合在一起。
- 修复了朝向倒置 Bug，长枪在抛出、收回等物理飞行过程中终于可以绝对精确地枪尖朝向飞行轨迹了。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1032`。
- 在参数面板增加明显的横向/纵向滚动条滑柄，提升移动端直观可操作性。
- 修复虚拟摇杆视觉表现：将摇杆中心小球的最大滑动距离限制在 `39px`，确保拉到满速时小球完全被约束在外圈基座范围内，不再越界。

- 修复长枪模型因为反转 180 度带来的“枪尖颠倒/倒着飞”问题，修正武器 Z 轴旋转方向使其真正保持物理与视觉方向一致。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1033`。

- 隐藏安卓打包相关文件与目录（`.tools`, `.build`, `*.apk`）至 `.android_hidden` 中。
- 修复玩家武器模型因为中心点偏移 (`position.y`) 导致在垂直方向上一直存在悬空/偏移的错误，将其正确映射至 Z 轴（飞行方向）。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1034`。

- 将敌人生成时烟雾 (`EnemySpawnEffects.js`) 的多边形细分等级提升（从 IcosahedronGeometry 的 0 提升到 2），使得粒子边缘更加平滑柔和。
- 全局检查模型平滑度：同步增加了粒子特效管理器中常规扬尘、玩家尾巴、玩家眼睛以及敌人眼睛的几何体 Segment（细分面）数量，以增强卡通渲染下的轮廓圆润度。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1035`。

- 拆分了角色的 `maxMoveSpeed` 配置项，分离为 `maxMoveSpeedX`（横向最大速度）与 `maxMoveSpeedZ`（纵向最大速度），以解决竖屏状态下横向移动在视觉上显得过快的问题。
- 将默认的横向最大速度 (`maxMoveSpeedX`) 降至 7.5，保留纵向最大速度 (`maxMoveSpeedZ`) 为 10。
- 在控制面板中开放了横纵向移动速度的独立控制。
- 更新 ControlPanel 面板版本号至 `v2026.04.09-1036`。

- 更新 ControlPanel 面板版本号至 `v2026.04.09-1036`，并重新打包输出最新安卓客户端（包含武器偏移修复、平滑多边形细分提升及分轴移动速度分离）。

- 弃用基于 apktool 反编译替换资源的出包方式，改为基于完整的 Android Shell 原生工程 (`.build/android-shell`) 并利用内部自带环境调用 `gradle assembleDebug` 构建原生的全新独立 APK 包，以避免可能带来的 webview 加载异常和资源映射路径损坏。
